package dd4.core.model

// Avoid using the class name "ObjectSet" as we want to be consistent with our own model, which cannot use "Object".
// TODO: Model is incomplete
data class ItemSet(val vnum: Int)
